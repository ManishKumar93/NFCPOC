<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="NFCPOC.Views.NFCPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Padding="10" Spacing="10">
            <Label Text="{Binding IsSupported, StringFormat = 'NFC IsSupported = {0}'}"></Label>
            <Label Text="{Binding IsAvailable, StringFormat = 'NFC IsAvailable = {0}'}"></Label>
            <Label Text="{Binding IsEnabled, StringFormat = 'NFC IsEnabled = {0}'}"></Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>